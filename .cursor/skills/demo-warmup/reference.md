# demo-warmup — implementation reference

Implemented:

- [`SKILL.md`](SKILL.md) — agent workflow and triggers
- [`scripts/demo-warmup.sh`](../../scripts/demo-warmup.sh) — `status` / `fix`
- [`scripts/cluster-lifecycle.sh`](../../scripts/cluster-lifecycle.sh) — `status` and `warmup` aliases
- Cross-links in `AGENTS.md`, `ROADMAP.md`, `demo-backstage-install`, `demo-backstage-prep`
