{
  "name": "agentops-example-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for OpenClaw + MLflow on RHOAI",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:openclaw": "npx playwright test openclaw-ui.spec.ts",
    "test:security": "npx playwright test sandbox-security.spec.ts",
    "test:mlflow": "npx playwright test mlflow-ui.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
